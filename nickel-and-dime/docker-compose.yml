version: '3.8'

services:
  frontend:
    build: .
    ports:
      - "5173:80"
    networks:
      - app-network

  # PHP backend for testing changes (optional)
  # Uncomment when you need to test PHP file changes locally
  # php-backend:
  #   build: ./php-files
  #   ports:
  #     - "8000:8000"
  #   environment:
  #     - DB_HOST=eredia.iad1-mysql-e2-9b.dreamhost.com
  #     - DB_NAME=nickel_and_dime_records
  #     - DB_USER=nadr_webuser
  #     - DB_PASS=_v4b_f8QCYBCGaY
  #   networks:
  #     - app-network

networks:
  app-network:
    driver: bridge